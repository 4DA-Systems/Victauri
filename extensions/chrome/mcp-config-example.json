{
  "_note": "The Victauri browser host requires a Bearer token (auth is on by default). Set a fixed token via VICTAURI_BROWSER_AUTH_TOKEN and put it in the Authorization header below, replacing <your-token>.",
  "mcpServers": {
    "victauri-browser": {
      "url": "http://127.0.0.1:7474/mcp",
      "headers": {
        "Authorization": "Bearer <your-token>"
      }
    }
  }
}
